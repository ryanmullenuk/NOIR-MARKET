{
  "name": "noir-market",
  "version": "9.7.0",
  "private": true,
  "description": "Noir Market browser game",
  "scripts": {
    "ios:prepare": "node scripts/prepare-ios.mjs",
    "check": "node --check game.js && node --check sw.js && node --test tests/*.test.mjs",
    "clean:legacy": "node scripts/dedupe-top-level-functions.mjs && node scripts/prune-obsolete-layers.mjs",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "acorn": "8.18.0",
    "jsdom": "30.0.1"
  }
}
